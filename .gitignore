node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# transient deploy bundle (built + sent, not source)
whywedie-dist.zip

# local env / secrets — never commit the real files; *.example templates are fine
.env
.env.*
!.env.example
!.env.deploy.example
.env.deploy
.secrets
